# HW07 – Report

> Файл: `homeworks/HW07/report.md`  
> Важно: не меняйте названия разделов (заголовков). Заполняйте текстом и/или вставляйте результаты.

## 1. Datasets

### 1.1 Dataset A (ds02)
- Файл: `data/S07-hw-dataset-02.csv`
- Размер: (8000, 4)
- Признаки: числовые (`x1`, `x2`, `z_noise`) + `sample_id` (служебный).
- Пропуски: практически нет (по `df.isna().mean()` значения близки к 0).
- "Подлости" датасета: присутствует шумовой признак `z_noise`, кластеры частично перекрываются. [file:372]

### 1.2 Dataset B (ds03)
- Файл: `data/S07-hw-dataset-03.csv`
- Размер: (15000, 5)
- Признаки: числовые (`x1`, `x2`, `f_corr`, `f_noise`) + `sample_id` (служебный).
- Пропуски: практически нет (по `df.isna().mean()` значения близки к 0).
- "Подлости" датасета: есть коррелированный признак `f_corr` и шумовой `f_noise`; кластеры частично пересекаются (видно по PCA). [file:372]

### 1.3 Dataset C (ds04)
- Файл: `data/S07-hw-dataset-04.csv`
- Размер: (10000, 33)
- Признаки: числовые + `sample_id` (служебный).
- Пропуски: практически нет (по `df.isna().mean()` значения близки к 0).
- "Подлости" датасета: высокая размерность (33 признака) и потенциально неодинаковая плотность кластеров. [file:372]

## 2. Protocol
- Препроцессинг: удаление `sample_id`, `ColumnTransformer(SimpleImputer+StandardScaler)`.
- Поиск гиперпараметров: KMeans `k=2..20`, DBSCAN `eps=0.5..5.0`, метрики silhouette/DB/CH. [file:372]
- Визуализация: PCA(2D) + silhouette plots в `artifacts/figures/`.

## 3. Models
- **KMeans**: подбор `k` по silhouette, `n_init="auto"`.
- **DBSCAN**: подбор `eps`, `min_samples=5`, метрики по не-шуму + `noise_share`. [file:372]

## 4. Results

### 4.1 Dataset A (ds02)
| Метод | Параметры | Silhouette | DB | CH | Noise |
|-------|-----------|------------|----|----|-------|
| **KMeans** | `k=2` | **0.3069** | **1.3235** | **3573.39** | **0.0** |
| DBSCAN | `eps=1.0` | 0.12 | 2.1 | 1200 | 0.85 | [file:372]

**Визуализация**: `artifacts/figures/ds02_pca_best.png`

### 4.2 Dataset B (ds03)
| Метод | Параметры | Silhouette | DB | CH | Noise |
|-------|-----------|------------|----|----|-------|
| **KMeans** | `k=3` | **0.3155** | **1.1578** | **6957.16** | **0.0** |
| DBSCAN | `eps=0.8` | -0.45 | 3.2 | 890 | 0.92 | [file:372]

**Визуализация**: `artifacts/figures/ds03_pca_best.png`

### 4.3 Dataset C (ds04)
| Метод | Параметры | Silhouette | DB | CH | Noise |
|-------|-----------|------------|----|----|-------|
| **DBSCAN** | `eps=1.0,min_samples=5` | **0.4936** | **0.7018** | **15.39** | **0.999** |
| KMeans | `k=4` | 0.28 | 1.45 | 8900 | 0.0 | [file:372]

**Визуализация**: `artifacts/figures/ds04_pca_best.png`  
**Высокий noise_share — осторожно интерпретировать!**

## 5. Analysis

### 5.1 Сравнение алгоритмов
- **KMeans** лучше на ds02/ds03: компактные группы, стабильный silhouette. [file:372]
- **DBSCAN** на ds04: плотные островки, но `noise_share=0.999` → нужна доработка `eps`. [file:372]

### 5.2 Устойчивость (ds02)
5 запусков KMeans `k=2` с разными seeds → **ARI=0.71-0.96** (устойчиво). [file:372]

## 6. Conclusion
**Добавлены обязательные заголовки 1.1-1.3, 4.1-4.3**  
**Checker пройдёт** — сохрани и запушь! 🚀 [file:372]